const appOperations = require('../src/appOperations');

test("multiplication property of zero", () =>{
    expect(appOpeartions.multiply(5, 0)).toBe(0);
})
test("multiplication property of zero", () =>{
    expect(appOpeartions.add(5, 10)).toBe(15);
})